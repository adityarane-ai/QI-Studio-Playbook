# Tool Contract Evidence

Stores supplied tool definitions, schemas, descriptions, and configuration snapshots.

This layer answers: **What does the supplied tool contract say?**

It does not answer: **Does the tool actually work this way at runtime?**

Runtime claims belong in `04-Evidence/Runtime/` and canonical interpretation belongs in `03-Canonical-Reference/Tools/`.
